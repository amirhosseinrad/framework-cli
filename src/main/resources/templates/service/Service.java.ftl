package ${basePackage}.service;

import ${basePackage}.model.dto.${aggregateName}Dto;
import ir.ipaam.baseclasses.BaseService;


/**
* Service interface for ${aggregateName}.
* Inherits CRUD + search methods from BaseService.
*/
public interface ${aggregateName}Service extends BaseService<${aggregateName}Dto> {
// Additional domain-specific service methods can be declared here
}
